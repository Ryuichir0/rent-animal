
 <div class="container">
  <div class="card-trip">
      <%= image_tag ('horses.jpg') %>
      <div class="card-trip-infos">
        <div>
          <h2><%= @herd.name %></h2>
          <p><%= @herd.species %></p>
          <p> <%= @herd.description %><p>
          <p> <%= @herd.address %><p>
          <%= link_to icon('fas fa-pen','edit'), edit_herd_path %>
          <%= link_to icon('fas fa-trash', 'remove'), herd_path, method: :delete, data: { confirm: "Are you sure?" } %>
        </div>
        <h2 class="card-trip-pricing"><%= @herd.unit_price%>€ /day</h2>
        <%= image_tag ('avatar.JPG'), :class => "card-trip-user avatar-bordered" %>
      </div>
  </div>
      <%= link_to 'return to Dashboard', herds_path,  class: "btn btn-flat"  %>
      <%= link_to 'Book this', new_herd_booking_path(@herd),  class: "btn btn-flat"  %>
      <%= link_to 'Bookings', herd_bookings_path(@herd),  class: "btn btn-flat"  %>
</div>
